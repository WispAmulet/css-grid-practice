{
  "name": "cssgrid.io",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js' --directory --port 7777",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yly",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.23.5"
  }
}
